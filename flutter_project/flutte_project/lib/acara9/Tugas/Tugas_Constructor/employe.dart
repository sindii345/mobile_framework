// Class Employee
class employe {
  // Deklarasi property
  int id;
  String name;
  String department;

  // Constructor untuk mengisi nilai property
  employe(this.id, this.name, this.department);
}