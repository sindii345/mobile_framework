void main() {
  var isThisWahyu = true;

  // Cara Biasa (If-Else Panjang)
  if (isThisWahyu) {
    print("wahyu");
  } else {
    print("bukan");
  }

  // Cara Ternary (Satu Baris)
  // Rumus: Kondisi ? (jika true) : (jika false)
  isThisWahyu ? print("wahyu") : print("bukan");
}
